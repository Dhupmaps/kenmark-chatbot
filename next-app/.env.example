# Next.js app environment
# FASTAPI_URL set this if your existing backend is on a different URL
FASTAPI_URL=http://127.0.0.1:8000
# ADMIN_KEY (optional) - forwarded to FastAPI admin endpoint
ADMIN_KEY=changeme
# NEXT_PUBLIC_ADMIN_KEY can be set when building frontend to expose admin uploading in dev
NEXT_PUBLIC_ADMIN_KEY=changeme
